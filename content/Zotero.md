# 文档管理

Zotero 的标签功能做得很烂，所以我决定尽量用集合功能来实现文档的分类整理。

# 文档阅读

到底还是没有舒服的 hover 和背单词呀。

# 移动/复制条目

如果仅仅是拖动文档到其他分类中，那么其实是“复制”，如果希望达到剪贴的效果，那么需要按住 `Command/Shift` 进行拖动。

# Connector

每次抓 ACM digital library 的时候经常报错，然后在 zotero settings -> 高级 -> 报告网络转换器故障 关掉，似乎就好了。

另外有的时候抓不到，是因为页面虽然显示出来了，但是并没有加载好。